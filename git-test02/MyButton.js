
// function MyButton() {
//     return <h1>Hello World!</h1>;//문자열이 아닌 태그가 삽입되어 있다.
//   }

//   const container = document.getElementById('root');
//   const root = ReactDOM.createRoot(container);
//   root.render(<MyButton />) //root에 랜더링 한다. 함수검색을 사용자 정의 태그로 한다. 